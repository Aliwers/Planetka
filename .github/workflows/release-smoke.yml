name: release smoke

on:
  workflow_dispatch:
  release:
    types: [published]

permissions:
  contents: read

jobs:
  install:
    runs-on: macos-15
    steps:
      - name: Install from the public command
        run: |
          curl -fsSL https://raw.githubusercontent.com/Aliwers/Planetka/main/install.sh | \
            PLANETKA_NO_OPEN=1 bash
      - name: Verify installed release
        run: |
          test -x /Applications/Planetka.app/Contents/MacOS/Planetka
          test "$(plutil -extract CFBundleIdentifier raw -o - /Applications/Planetka.app/Contents/Info.plist)" = "com.local.planetka"
          test "$(plutil -extract CFBundleShortVersionString raw -o - /Applications/Planetka.app/Contents/Info.plist)" = "0.2.40"
          file /Applications/Planetka.app/Contents/MacOS/Planetka | grep -q arm64
          codesign --verify --deep --strict /Applications/Planetka.app
          codesign -d --entitlements :- /Applications/Planetka.app > "$RUNNER_TEMP/entitlements.plist" 2>/dev/null
          test "$(/usr/libexec/PlistBuddy -c 'Print :com.apple.security.device.audio-input' "$RUNNER_TEMP/entitlements.plist")" = "true"
          test "$(/usr/libexec/PlistBuddy -c 'Print :com.apple.security.device.microphone' "$RUNNER_TEMP/entitlements.plist")" = "true"
