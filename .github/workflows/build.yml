name: build

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  macos:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - name: Validate scripts and plists
        run: ./scripts/check.sh
      - name: Swift self-tests
        run: swift run -c debug --package-path swift Planetka --self-test all
      - name: Build app bundle
        run: ./scripts/build-app.sh "$RUNNER_TEMP/Planetka.app"
      - name: Verify app bundle
        run: |
          test -x "$RUNNER_TEMP/Planetka.app/Contents/MacOS/Planetka"
          codesign --verify --deep --strict "$RUNNER_TEMP/Planetka.app"
      - name: Exercise installer and uninstaller
        run: |
          mkdir -p "$RUNNER_TEMP/release"
          ditto -c -k --sequesterRsrc --keepParent \
            "$RUNNER_TEMP/Planetka.app" "$RUNNER_TEMP/release/Planetka.zip"
          checksum="$(shasum -a 256 "$RUNNER_TEMP/release/Planetka.zip" | awk '{print $1}')"
          PLANETKA_RELEASE_URL="file://$RUNNER_TEMP/release/Planetka.zip" \
          PLANETKA_RELEASE_SHA256="$checksum" \
          PLANETKA_NO_OPEN=1 \
            ./install.sh
          test -x /Applications/Planetka.app/Contents/MacOS/Planetka
          codesign --verify --deep --strict /Applications/Planetka.app
          ./uninstall.sh
          test ! -e /Applications/Planetka.app
